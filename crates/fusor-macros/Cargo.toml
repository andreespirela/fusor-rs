[package]
name = "fusor-macros"
description = "FromInputs and JsInputs derives for fusor components"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["fusor", "derive", "wasm", "web", "components"]
categories = ["wasm", "web-programming"]

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "=1.0.107"
quote = "=1.0.47"
syn = { version = "=2.0.119", features = ["full", "fold"] }

[lints]
workspace = true
